{
  "name": "@security-alliance/taxii",
  "version": "0.0.1",
  "description": "TAXII client written in JavaScript",
  "main": "index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier:check": "npx prettier -c \"**/*.{js,ts,md,json,yml,yaml}\"",
    "prettier:fix": "npx prettier -w \"**/*.{js,ts,md,json,yml,yaml}\""
  },
  "keywords": [
    "STIX",
    "TAXII"
  ],
  "author": "Security Alliance",
  "license": "MIT",
  "devDependencies": {
    "mocha": "^10.7.3",
    "prettier": "^3.3.3",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "@security-alliance/stix": "^0.0.1"
  }
}
